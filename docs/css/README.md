# CSS Guide

> CSS 작성 규칙은 현재 [Markup / CSS 가이드](../markup/README.md)의 CSS 섹션에 통합해서 관리합니다.

CSS는 HTML 구조, 클래스 네이밍, 접근성과 분리해서 보기 어렵습니다. 그래서 이 저장소에서는 `docs/css/`를 별도 깊은 문서로 늘리기보다, 우선 `docs/markup/README.md`에서 함께 관리합니다.

바로 이동:

- [Markup / CSS 가이드 보기](../markup/README.md)

추후 CSS 정렬 도구나 자동 변환 도구가 생기면 `tools/` 아래에 별도로 관리할 수 있습니다.
